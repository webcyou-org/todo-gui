<Window x:Class="ToDo.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:prism="http://prismlibrary.com/"
        xmlns:controls="clr-namespace:ToDo.Views.Controls"
        prism:ViewModelLocator.AutoWireViewModel="True"
        Title="ToDo" Width="800" Height="600"
        Background="{StaticResource BrushBG}"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="24,10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Title -->
        <TextBlock Grid.Row="0"
                   Text="ToDo"
                   Foreground="{StaticResource BrushWhite}"
                   FontSize="18"
                   FontWeight="Bold"
                   Margin="0,0,0,14" />

        <!-- Input -->
        <controls:TodoInputControl Grid.Row="1" Margin="0,0,0,14" />

        <!-- Tabs -->
        <controls:TabMenuControl Grid.Row="2" Margin="0,0,0,3" />

        <!-- Todo List -->
        <controls:TodoListControl Grid.Row="3" />
    </Grid>
</Window>